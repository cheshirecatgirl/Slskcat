[package]
name = "slskcat-core"
description = "Protocol-agnostic core for the slsk.cat Soulseek client"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
soulseek-rs-lib = "=12.0.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
