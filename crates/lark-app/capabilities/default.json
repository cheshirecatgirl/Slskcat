{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the Lark window needs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
